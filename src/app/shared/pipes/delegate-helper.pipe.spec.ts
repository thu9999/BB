import { DelegateHelperPipe } from './delegate-helper.pipe';

describe('DelegateHelperPipe', () => {
  it('create an instance', () => {
    const pipe = new DelegateHelperPipe();
    expect(pipe).toBeTruthy();
  });
});
